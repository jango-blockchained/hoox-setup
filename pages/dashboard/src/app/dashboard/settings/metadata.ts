import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Settings | Hoox Trading System",
  description:
    "Manage system settings, configure kill switch, and update notification preferences.",
}
