openapi: "3.0.3"
info:
  title: Hoox API
  version: "1.0.0"
servers:
  - url: https://api.hoox.sh
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
