{
  "name": "web3-wallet-worker",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare worker for Web3 wallet operations using ethers.js",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.13.2",
    "bun-types": "^1.2.10",
    "@types/jest": "^29.5.14",
    "jest-mock": "^29.7.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.2",
    "ethers": "^6.13.7"
  }
}
